# Optional: pip install pre-commit && pre-commit install
# Runs the same Docker-based hassfest as .github/workflows/ci.yml before each commit.
repos:
  - repo: local
    hooks:
      - id: hassfest
        name: hassfest (Docker)
        entry: scripts/hassfest.sh
        language: script
        pass_filenames: false
